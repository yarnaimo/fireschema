import { STypes } from '../../types/index.js';
import { DataModel } from '../model.js';
export declare const renderEntities: ($allow: STypes.AllowOptions, model: DataModel<any, any, any> | undefined, collectionPath: string) => string | null;
