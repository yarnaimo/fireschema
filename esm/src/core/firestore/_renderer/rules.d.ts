import { STypes } from '../../types/index.js';
import { DataModel } from '../model.js';
export declare const renderRules: ($allow: STypes.AllowOptions, model: DataModel<any, any, any> | undefined, pIndent: number) => string | null;
